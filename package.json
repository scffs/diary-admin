{
  "name": "diary-teacher",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install && pnpm run format",
    "format": "prettier --write ."
  },
  "workspaces": [
    "apps/*"
  ],
  "keywords": [],
  "author": "scffs",
  "license": "MIT",
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.1.0",
    "prettier": "^3.1.0",
    "typescript": "*"
  }
}
